# Testing

Run the focused suite with `python3 -m unittest discover -s tests`. Tests cover database transactions, authentication, ownership, matching, analytics, roadmap calculations, job preparation, resume tailoring, PDF generation, and voice metadata persistence.

Manual checks should include desktop/mobile layouts, authenticated ownership boundaries, empty states, AI failures, database failures, invalid uploads, voice-supported browsers, unsupported browsers, and PDF downloads.
